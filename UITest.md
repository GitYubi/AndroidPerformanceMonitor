## UI 测试内容

在原有性能测试页面的基础上，利用midscene结合UI测试框架，新增UI功能测试内容。具体功能需求如下：

1. 通过自然语言，让工具根据当前车机软件的UI结构进行目标搜索、操作并判断结果。以下回提供一些示例，具体的测试内容包括但不限于以下类型：
   eg.1 “打开WLAN的开关，搜索名为YuBiL的WIFI，输入密码${WIFI_PASSWORD}并连接，检查是否连接成功。
   执行：打开APPList - 打开系统设置 - 选择WLAN开关所在的页面 - 如果WLAN开关没打开，则打开对应的开关 - 找到名为YuBiL的WIFI - 点击对应WIFI，输入密码${WIFI_PASSWORD} - 等待连接完成，并检查页面是否成功连接WIFI并发生对应变化。
   eg.2 “页面设置为深色模式，检查一下页面是否正常”
   执行：打开APPList - 打开系统设置 - 选择深色模式切换设置所在的页面 - 如果页面不是深色模式切换为深色模式 - 检查地图、系统界面等常用页面内容是否切换至深色模式
   eg.3 “根据系统设置页面进行探索测试”
   执行：根据所有目标页面和页面内的开关进行开启、关闭测试，检查页面变化。
2. 将执行过的步骤保存成脚本，供用户在前端界面挑选后重复执行。保存后的脚本可修改名称、设置分组或标签、删除、多选导出Zip文件。
3. 根据执行结果，可生成对应的成功、失败报告。
4. 在UI自动化测试页面内增加设置按钮，供用户进行报告输出路径、midscene环境配置

要求：
1. 选择合理的测试框架，融入midscene的能力
2. 可改变现有的页面框架，通过标签、选项卡的方式更换测试页面
3. 保证性能监控和UI测试可同时执行

## 已确认的实施约束（2026-09-15）

- 车机主要通过 USB ADB 连接，所有执行任务显式绑定设备序列号。
- 使用多模态模型理解截图、定位操作目标和判断结果；首轮使用 DeepSeek 官方接口 `https://api.deepseek.com`，模型 `deepseek-flash`，截图通过该云端接口处理。
- 保存后的脚本支持逐步编辑，不只保存原始自然语言或点击坐标。
- 保留 React + FastAPI 性能监控主体，新增独立 Node.js UI 执行进程。
- 第一版先实现单设备、单 UI 任务；允许同设备性能采样同时运行。

## 实施步骤与验收条件

按以下顺序实施。每步记录实际验证结果，未做的真机验证不能标为通过。

### Step 1：接入验证与框架定版

工作：
- 检查 Node.js、ADB、USB 设备授权及目标屏幕截图能力。
- 确认多模态服务、模型名、接口地址和凭据的本机配置方式。
- 用最小脚本验证截图理解、单次点击、文本输入、等待与断言、报告输出。
- 优先评估 Midscene Test + @midscene/android，核实包版本与 Node.js 兼容性并锁定依赖；Beta 能力不满足时保留 Android SDK，使用独立执行适配层。
- 验证车机横屏、实际分辨率、中文输入和系统设置页；若涉及多屏，明确首版目标显示屏。

交付：可运行的最小接入脚本、配置示例、版本记录和实际验证记录。

验收：在目标 USB 车机完成“观察 → 操作 → 断言 → 报告”闭环；模型或设备未准备好时，明确保留待验证项。

当前检查：本机存在 ADB 36.0.0、Node.js v26.7.0；设备列表仅发现 emulator-5554，未发现 USB 车机。尚未验证 Midscene、模型调用和目标车机操作。

首轮模型配置已确定：
- `MIDSCENE_MODEL_BASE_URL=https://api.deepseek.com`
- `MIDSCENE_MODEL_NAME=deepseek-flash`
- `MIDSCENE_MODEL_FAMILY=deepseek`
- `MIDSCENE_MODEL_API_KEY` 由本机环境提供，不写入前端、文档或版本库。
- 配置模板：`tools/ui-test/.env.example`。复制到同目录 `.env` 后在本机填写密钥；后续执行器需显式加载该文件，现有性能监控启动脚本不会自动加载它。
- 2026-09-15 官方文档核对：Midscene 从 v1.12.0 起支持 DeepSeek，并明确列出上述模型配置。版本兼容性和真实调用仍需实测，不将文档支持记为接入成功。
- 参考：[Midscene 模型配置](https://www.midscenejs.com/model-common-config)、[DeepSeek V4.1 Flash 发布说明](https://api-docs.deepseek.com/news/news260910/)。

### Step 2：定义可编辑用例和执行协议

工作：
- 定义用例 ID、版本、名称、分组、标签、参数、前置条件、步骤及清理步骤。
- 步骤包含稳定 ID、类型、自然语言内容、参数引用、超时、失败策略和启用状态。
- 首版步骤类型：操作、输入、等待条件、断言、截图；自动重试仅在可安全重复的步骤显式启用。
- 区分通过、断言失败、执行异常、取消、待复核，避免将模型或设备错误算作产品缺陷。
- 定义执行事件：任务 ID、步骤 ID、时间、状态、错误和证据路径。
- 每次运行保存用例版本快照，编辑用例不改变历史报告和正在执行的任务。

交付：数据结构、接口契约、Wi-Fi 与深色模式示例用例。

验收：示例可被校验和序列化；非法步骤被拒绝；密码通过参数引用注入，不默认写入导出文件。

### Step 3：建立后端任务执行闭环

工作：
- FastAPI 管理 UI 任务，Node.js 子进程执行 Midscene，通过结构化事件传递进度。
- 实现创建、查询、取消、超时、设备断开和进程退出处理。
- 同设备 UI 操作互斥，性能采样使用独立任务生命周期。
- 持久化任务、步骤和证据；服务重启后将遗留运行任务标为中断。
- 模型配置按任务快照加载；凭据留在服务端，不返回给前端或普通日志。

交付：任务 API、执行适配层、存储及关键异常路径测试。

验收：通过 API 完成一次用例；取消后无残留执行进程；异常有明确终态和原因；不阻塞性能采样接口。

### Step 4：增加 UI 自动化页面和设置

工作：
- 增加“性能监控 / UI 自动化”一级导航和持续可见的运行状态。
- UI 页面提供设备选择、自然语言输入、执行、取消、实时步骤及截图证据。
- 设置提供模型服务、模型名、凭据配置、连接检查和报告输出目录。
- 输出目录指测试主机文件系统路径，由后端校验可写性。
- 切页和刷新后恢复任务状态，避免前端组件卸载影响后端任务。

交付：可用的执行页面与设置面板。

验收：从页面发起测试并看到真实进度和结果；切换页面、刷新后仍可查看和取消任务。

### Step 5：实现逐步编辑、用例库和复跑

工作：
- 把执行过的有效操作整理为可编辑步骤草稿；保留原始轨迹供核对，不将报告或缓存直接当作脚本。
- 支持新增、删除、排序、启停步骤，以及修改动作、输入参数、等待条件、断言和超时。
- 保存名称、分组、标签及版本，支持删除和多选 ZIP 导出。
- 导出包含结构化用例、参数模板、格式版本和运行说明；明确其所需执行器，不承诺 ZIP 本身可独立执行。
- Wi-Fi 用例明确“已连接目标 SSID”断言；互联网连通性作为单独检查项。
- 深色模式用例明确目标应用、页面清单及各页检查条件。

交付：步骤编辑器、用例库、版本快照与导出功能。

验收：完成“执行 → 保存 → 修改一个步骤或参数 → 复跑”；实际运行采用修改后的步骤，历史记录保留原版本。

### Step 6：报告与性能监控联动

工作：
- 汇总任务、步骤、预期结果、实际结果、失败原因和截图，关联 Midscene 原始报告。
- 用主机时间和性能会话 ID 对齐 UI 步骤与采样区间，允许只运行 UI 或只运行性能监控。
- 协调现有结束时导出并清理设备日志的行为，避免另一任务运行期间丢失证据。
- 对比单独执行 UI 测试与并行采样时的耗时、采样间隔和超时情况，记录实际开销。

交付：报告列表、报告详情、步骤与性能时间关联及并行验证记录。

验收：两类任务可同时运行并分别停止；页面切换不影响执行；报告证据完整；真实测量并行开销，不以“能启动”代替验证。

### Step 7：受限探索测试

工作：
- 指定入口、允许页面、控件范围、最大步骤、深度和时长。
- 记录已访问页面与控件状态，避免重复循环；在可行时恢复原始状态。
- 排除恢复出厂、清除数据、关闭调试等会破坏后续测试的操作。
- 将探索发现转成可编辑的回归用例草稿。

交付：探索配置、执行流程与覆盖记录。

验收：在限定范围内完成探索或按预算停止；报告准确区分已检查、跳过和未覆盖内容，不宣称遍历整个系统。

## 推进原则

- 第一阶段完成 Step 1–4，形成可操作、可观测的执行闭环。
- 第二阶段完成 Step 5–6，满足脚本管理、复跑、报告和并行监控要求。
- 第三阶段完成 Step 7，扩展探索能力。
- 每一步结束记录改动、验证证据、未解决问题和下一步；基础能力未验证时，不以模拟结果替代真机结论。

## 2026-09-16：测试前页面结构扫描

新增独立阶段：**页面结构扫描 → 人工查看范围和覆盖缺口 → 编辑测试步骤 → 执行与多模态断言**。

### 当前实现

- UI 自动化工作台新增“页面结构”入口，选择 ADB 设备、允许包名、菜单范围、页面数、深度和动作预算。
- 通过 `uiautomator dump` 获取当前窗口 XML，解析控件父子关系、文字、resource-id、类名、坐标、可点击/可勾选状态。
- 连续读取结构以等待页面稳定。使用确定性深度优先遍历；模型不选择页面或规划路径，扫描无需模型凭据。
- 自动模式仅选择可识别的菜单容器，排除开关及包含开关的行、输入框与受限操作；也可指定允许的菜单名称。
- 每次点击重新解析并唯一匹配控件；返回后校验父页面指纹，位置不一致立即停止并保存证据。
- 保存页面 JSON、跳转关系及覆盖记录；前端展示页面导航树和可展开的单页控件树，支持重新查看历史扫描和下载结构索引。
- 扫描与正式测试分开；扫描不能包含开关操作或模型断言。同设备共用任务互斥、取消与超时机制。

### 能力边界

Android 当前窗口控件树不是整个应用的路由表，无法在不进入页面的情况下保证获得“所有页面”。本版收集指定范围内**已发现页面的可见控件**；滚动区域、未识别菜单、自绘视图、未授权包名和预算外页面不保证覆盖。页面图允许重复入口和环，导航树按首次发现展示。

下一步补充滚动区域的分段发现与位置恢复、车机专用菜单规则、从页面树选择控件生成可编辑步骤。多模态模型继续用于正式测试的视觉判定，不参与结构扫描。

### 已有验证证据

- Android 11 模拟器 `emulator-5554` 已验证 DeepSeek 画面断言、设置搜索与中文输入、用例修改复跑、与性能采样并行。
- 首次结构扫描生成设置首页（73 个控件）及“已连接的设备”（31 个控件），验证返回不一致时停止并标记待复核。
- 尚未接入实际 USB 车机，模拟器结果不替代车机兼容性验证。
- 增加连续结构稳定性检查后，限定“显示、声音”的扫描完成 3 页（73 + 50 + 66 = 189 个控件节点），4 次导航动作，两次返回均校验通过。运行 ID：`b4ac1d8610a546b684a2b265e3960498`。滚动区域仍标为未覆盖。
- 回归验证：后端 44 项、Node 执行器 15 项通过；TypeScript 类型检查和前端生产构建通过。构建仍有原项目 analytics 环境变量与大资源包提示。
